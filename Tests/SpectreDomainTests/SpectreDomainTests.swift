import XCTest
@testable import SpectreDomain

final class SpectreDomainTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SpectreDomain().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
